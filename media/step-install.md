# Install Claude Code

MacroDeploy runs the **Claude Code CLI** on your machine — your code never leaves it.

If you don't have it yet:

```bash
npm install -g @anthropic-ai/claude-code
```

Then click **Set Up / Sign In** and choose **"Install via npm"** or **"I've installed it"**.
